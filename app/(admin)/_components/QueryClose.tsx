'use client';

import { Button } from "@/components/ui/button";
import { toast } from "sonner";


const QueryClose = () => {
  return (
    <Button onClick={() => toast.info('Not implemented yet!')}>Close</Button>
  )
}

export default QueryClose